server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: dev

---
spring:
  config:
    activate:
      on-profile: dev
  cloud:
    config:
      server:
        git:
          uri: "file:///media/maathan/af6f6db7-93ac-4d81-9215-393a630b20a4/Linux software data/intellij/educore-config/"
          default-label: main

---
spring:
  config:
    activate:
      on-profile: prod
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nikhilmathew320/educore-configs.git
          default-label: main